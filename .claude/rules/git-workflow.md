# Git / PR の進め方

実装者向けの運用ルール。レビュアーの判断基準ではないので `AGENTS.md` には置かない。

## ブランチと PR

- ブランチは `main` から切る（`feat/` `fix/` `chore/` `refactor/` `docs/`）
- **`main` へ直接 push しない。** 必ず PR 経由
- PR は 1 つの関心事にひとつ。レビューしやすい大きさに割る
  （このプロダクト自体がそういう思想のツールである）
- PR タイトル・本文は日本語。本文には「何を変えたか」ではなく
  **「なぜ変えたか」と「どう検証したか」**を書く
- マージは squash。PR 1 つが `main` の 1 コミットになる

## コミットメッセージ

**件名は gitmoji + 英語、本文は日本語。**
件名は `git log --oneline` や GitHub の一覧に並ぶので英語で揃え、詳細は読み手に合わせる。

```
<gitmoji> <type>: <subject>

<本文（日本語）: なぜこの変更が必要だったか。何をしたかは diff が語る>
```

件名は命令形・小文字始まり・72 文字以内・末尾にピリオドを打たない。
絵文字は Unicode 文字そのものを使う（`:sparkles:` ではなく `✨`）。
本文は書くことがなければ省略してよい。

`🎉 init` / `✨ feat` / `🐛 fix` / `♻️ refactor` / `✅ test` / `📝 docs` / `🔧 chore` /
`👷 ci` / `⬆️ deps` / `🔥 remove` / `🔒️ security` / `⚡️ perf` / `💄 ui`

## 作業を終える前に

`./task check` を通す。加えて、**シークレット・絶対パス・個人情報がコミットに
含まれていないこと**と、**実装に合わせて `AGENTS.md` / `.env.example` を直したこと**を確認する。
