# CLAUDE.md

このリポジトリの規約は `AGENTS.md` が正。以下でその内容を読み込む。

@AGENTS.md

<!--
CLAUDE.md を AGENTS.md へのシンボリックリンクにする手もあるが、
import 形式にしておくと Claude Code 固有の指示を下に足せる。
規約の本体は AGENTS.md 側にだけ書き、ここに複製しないこと。
-->

## Claude Code 固有の指示

- **ホストでコマンドを実行しない。** `pnpm` / `npx` / `node` は必ず `./task` 経由でコンテナ内で動かす。コンテナが落ちていれば `./task` が自動起動する。
- **`node_modules/` と `.next/` を Grep / Glob の対象にしない。** 巨大でノイズになる。
- **調査メモ・作業ログ・実装サマリーの `.md` をリポジトリに作らない。** 一時ファイルはスクラッチパッドディレクトリに置く。
- **`./task check` は最後にまとめてではなく、こまめに回す。** 失敗の原因を小さく保つため。
