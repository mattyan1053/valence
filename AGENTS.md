# AGENTS.md

このリポジトリで作業するすべての AI エージェント（Claude Code / Codex / その他）向けの共通指示。
人間の新規参加者が読んでも同じ内容で通用するように書いてある。

**ここには「コードを読んでも分からないこと」だけを置く。** 依存の一覧は `package.json`、
コマンドの一覧は `./task help`、ディレクトリ構成は実物が正。重複させない。

---

## 0. 応答言語

**応答・PR タイトル / 本文・レビューコメント・Issue コメントは日本語。**
英語で指示されても出力は日本語に揃える。識別子と型名は英語。コードコメントは日本語でよい。

例外は**コミットメッセージの件名だけ**（§7 参照。本文は日本語）。

---

## 1. プロダクト

**Valence** — AI 時代の PR コントロールセンター。

AI によるコード生成が速くなった結果、ボトルネックは「書く」ではなく「レビューしてマージする」へ移った。
Valence は GitHub を**置き換えず拡張する** GitHub App として、レビュアー側の交通整理を担う。

MVP は**決定論的に判断できるものだけ**を扱う:

1. **依存グラフ** — PR の base/head トポロジーから PR 間の関係を DAG として描画
2. **ルールベースのトリアージ** — 変更ファイル・行数・CI 通過状況から静的にリスク Tier を判定
3. **1 クリック Approve / Merge**

**今は作らない:** LLM による要約・自動レビュー、GitHub 以外への対応、課金・組織管理。
本番は Vercel にデプロイする想定（Dockerfile は開発専用で、本番では使われない）。

---

## 2. 迷ったときの技術選択

一覧は `package.json` が正。ここには**間違えやすい選択**だけ書く。

- パッケージマネージャは **pnpm**。npm / yarn を使わない
- lint / format は **Biome**。ESLint / Prettier を入れない
- テストは **Vitest**。バリデーションは **Zod**（境界での入力検証に必ず使う）
- TypeScript は `strict` + `noUncheckedIndexedAccess` + `verbatimModuleSyntax`
- ランタイムは **Node 24**（Active LTS）。26 は LTS 前かつ corepack 非同梱なので上げない

---

## 3. 開発環境

**ホストには何もインストールしない。すべてコンテナ内で実行する。**
ホストに要求するのは **bash / docker / git だけ**。コマンド一覧は `./task help`。

```bash
./task up      # 起動 = アプリが動く (http://localhost:3000)
./task check   # lint + typecheck + 依存方向 + test（コミット前に必ず）
```

ホストで `pnpm` / `npx` / `node` を直接実行しないこと。
コンテナの `CMD` が `pnpm dev` なので、**起動していればアプリは動いている**。
依存の導入は entrypoint が面倒を見るため、clone 直後でも `./task up` だけでよい。

手元 PC からは SSH ポートフォワードで繋ぐ:

```bash
ssh -L 3000:localhost:3000 -L 54321:localhost:54321 <user>@<remote-vm>
```

### Supabase ローカルスタックの落とし穴

起動するのは **db / auth / rest / kong の 4 つだけ**（`supabase/config.toml` で他は無効）。
MVP で使わないうえ、イメージだけで数 GB になるため。

- **接続先が 2 通りある。** サーバー側からは docker network 経由で `http://kong:8000`。
  ブラウザ側（`NEXT_PUBLIC_*`）はコンテナ名を解決できないので `http://localhost:54321`。
  取り違えると原因が分かりにくい
- **CLI 専用の使い捨てコンテナ（`supabase-cli`）がある。** CLI は docker socket と
  ホストのネットワーク名前空間の両方を要求するので、`app` に持たせず切り出してある。
  **`app` に docker socket や `network_mode: host` を戻さないこと**
- **`supabase-cli` から `env_file` を外さないこと。** `config.toml` の `env()` は
  このコンテナのプロセス環境から解決される
- 認証は **GitHub OAuth のみ**。メールでのサインアップは塞いである

---

## 4. アーキテクチャ

DDD / クリーンアーキテクチャの**依存関係逆転**だけを採る。形式主義には陥らない。
守るのは「ビジネスルールがフレームワークに依存しない」の一点。

```
src/
  app/              Next.js App Router。ルーティングと配線のみ
  domain/           ビジネスルール。外部依存ゼロ
  application/      ユースケース。ports/ に interface を置き、実装は知らない
  infrastructure/   ports の実装（GitHub GraphQL、Supabase 等）
  ui/               React コンポーネント。表示に専念する
  composition/      合成ルート。ports に adapter を束ねる唯一の場所
```

内側（`domain`）ほど安定し、外側（`app`）ほど変わりやすい。依存の矢印は常に内向き。

| レイヤ | import してよい先 |
| --- | --- |
| `domain` | Node 標準ライブラリと `domain` 自身**のみ** |
| `application` | `domain` と Node 標準ライブラリ**のみ**（npm も不可） |
| `infrastructure` | `application`, `domain`, npm |
| `ui` | `domain`, 他の `ui`, React |
| `composition` | すべて |
| `app` | `composition`, `ui`, `application`, `domain` |

違反は `./task check`（`.dependency-cruiser.mjs`）で落ちる。テストファイルは対象外。
**ルールを緩めて通すのではなく、設計を直すこと。** 例外を足すなら理由をコメントに書く。

- **ドメインは純粋関数で書く。** GitHub API のレスポンス型ではなく自前のドメイン型を入力に取る
- **外部データは境界で Zod 検証し、ドメイン型へ変換してから内側へ渡す。** 境界で `any` / 未検証の `as` を使わない
- **副作用は infrastructure に閉じ込める。** `fetch` / Supabase クライアント / `process.env` が
  domain・application に出てきたら設計ミス
- `application` で Octokit や Supabase SDK を import したくなったら**port の設計漏れ**

---

## 5. テストファースト

**新しい振る舞いを追加するときは、必ずテストを先に書く。**

1. **Red** — 失敗するテストを書き、**実際に落ちることを確認する**
2. **Green** — 通すための最小限の実装
3. **Refactor** — 緑のまま設計を整える

- ドメインロジックにはテストが**必ず**要る。カバレッジ閾値は CI で強制する
- `*.test.ts` をテスト対象と同じディレクトリに置く（co-location）
- テスト名は日本語で「何をすると何になるか」。例: `it("base ブランチが未マージの PR は要注意 Tier になる")`
- 外部 I/O はモックせず、`ports` に対するインメモリ実装を使う。`vi.mock` は最後の手段
- バグ修正も**先に落ちる回帰テストを書いてから**直す

---

## 6. コードとドキュメントをクリーンに保つ

- **命名がすべて。** コメントで補う前に名前を変える
- **関数は 1 つのことをする。** ネストが 3 段を超えたら早期リターンか関数抽出
- **推測でコードを足さない（YAGNI）。** 今の要件にない抽象化・設定項目を先回りで作らない
- **重複は 3 回目に抽象化する。** 2 回目のコピペは許容
- **死んだコードと後方互換の残骸は消す。** 誰も使っていない機能に「移行期間」は要らない
- **`any` を使わない。** 型がつかないなら `unknown` で受けて絞り込む
- 既存ファイルを編集するときは周囲のスタイルに合わせる

**仕様の正はコードである。** 仕様書・設計書・ADR は作らない。
書いてよいのは外部サービスのセットアップ手順など、コードから読み取れないことだけ。
作業ログ・調査メモ・実装サマリーを**リポジトリにコミットしない**。

---

## 7. Git / PR

- ブランチは `main` から切る（`feat/` `fix/` `chore/` `refactor/`）。**`main` へ直接 push しない**
- PR は 1 つの関心事にひとつ。**本文には「なぜ変えたか」と「どう検証したか」**を書く
- マージは squash。PR 1 つが main の 1 コミットになる

### コミットメッセージ

**件名は gitmoji + 英語、本文は日本語。** 件名は一覧に並ぶので英語、詳細は読み手に合わせる。

```
<gitmoji> <type>: <subject>

<本文（日本語）: なぜ必要だったか。何をしたかは diff が語る>
```

件名は命令形・小文字始まり・72 文字以内・末尾にピリオドを打たない。絵文字は Unicode 文字そのもの。

`🎉 init` / `✨ feat` / `🐛 fix` / `♻️ refactor` / `✅ test` / `📝 docs` / `🔧 chore` /
`👷 ci` / `⬆️ deps` / `🔥 remove` / `🔒️ security` / `⚡️ perf` / `💄 ui`

### Codex によるレビュー

PR 作成時に Codex が自動でレビューする。再レビューは `@codex review` とコメントする。

Codex へ: **レビューコメントは日本語で。** 優先順は
①正しさのバグ ②セキュリティ（§8） ③境界違反・テストの欠如 ④可読性・命名。
スタイルは Biome が見るので指摘しなくてよい。

**返信は各インラインコメントへ個別に返すこと**（PR 全体へのコメント 1 件にまとめない）。

```bash
gh api --method POST repos/<owner>/<repo>/pulls/<n>/comments/<comment-id>/replies -f body='...'
```

**打ち切り基準。** 再レビューは回すたびに新しい指摘が出て終わらない。以下を満たしたらマージしてよい:

- ①正しさと②セキュリティの指摘が残っていない
- ③に対応済み、または「対応しない理由」をコメント済み
- 再レビューは原則 **2 周まで**。3 周目の新規 nit は Issue に落とすか見送る

「指摘がゼロになるまで」を目標にしない。**マージを止めてよいのは正しさとセキュリティだけ。**

---

## 8. セキュリティ（public リポジトリ）

**このリポジトリは public。**

- **絶対にコミットしないもの:** GitHub App の秘密鍵 (`*.pem`)、App ID / Client Secret、
  Webhook Secret、Supabase の secret キー、アクセストークン、smee のチャンネル URL、
  個人のメールアドレス・ホスト名・絶対パス
- シークレットは `.env`（gitignore 済み）。`.env.example` にはキー名とダミー値だけ
- 設定ファイルにホストの絶対パスを埋め込まない（`${PWD}` などで解決する）
- GitHub Webhook は必ず署名（`X-Hub-Signature-256`）を検証してから処理する
- ユーザーが閲覧権限を持つリポジトリのデータしか返さない。RLS を有効にし、secret キーを
  クライアントへ渡さない
- 外部入力（Webhook ペイロード、API レスポンス、クエリパラメータ）は Zod で検証してから使う
- ログにトークン・秘密鍵・個人情報を出さない

**ローカル Supabase のポートについて。** CLI はポート（54321 / 54322）を必ず `0.0.0.0` に
publish し、bind アドレスを変える設定は無い。Docker の port publish は firewalld も迂回する。
**開発 VM が直接インターネット公開されていない前提で許容している。** 公開ホストで上げないこと。

---

## 9. 作業を終える前に

`./task check` を通す。加えて、**シークレット・絶対パス・個人情報がコミットに
含まれていないこと**と、**実装に合わせて `AGENTS.md` / `.env.example` を直したこと**を確認する。
